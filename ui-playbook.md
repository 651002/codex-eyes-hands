# UI 操作剧本库（命令/快捷键优先）

> 用途：watch/ask 等 GUI 任务提示词会要求先读本文件，找现成的高效操作路径，避免逐屏截图探索。
> 维护规则：每完成一次 GUI 任务，把**验证过的**最短路径记到对应条目；没验证过的标「待验证」。
> 铁律：能命令/脚本/快捷键的，永远不用鼠标逐像素点击。

## QQ（桌面版）

**发消息给指定好友（已验证，2026-XX-XX 空客）**
1. `Ctrl+F` 打开 QQ 搜索框 → 输入好友昵称 → `Enter` → 打开搜索结果第一个（单击或回车）。
2. 光标应已落在聊天输入框 → 直接输入消息文本。
3. `Ctrl+Enter` 发送（QQ 默认 Ctrl+Enter 发送 / Enter 换行；若用户改过设置则点「发送」按钮）。
4. 验证：聊天记录区最后一条应出现刚发的消息；用窗口快照/读取确认。

**注意事项**
- 昵称可能有重复：先用搜索看结果数量，唯一结果直接回车，多个结果按用户指定的备注选。
- QQ 主面板搜索框快捷键 Ctrl+F 待验证（若无效则点击顶部搜索框）。
- 发送前不要询问用户确认（用户已全权授权）；发送后验证并报告。

## 通用快捷键（Windows）

- 切换窗口 `Alt+Tab`；打开开始菜单 `Ctrl+Esc`；运行 `Win+R`；资源管理器 `Win+E`。
- 浏览器：地址栏 `Ctrl+L`，新标签 `Ctrl+T`，查找 `Ctrl+F`。

## 待办（下次遇到新应用时补充）

- 微信：待验证。
- 记事本：已验证路径——打开后直接 `Ctrl+S` 保存到指定路径即可（无需 GUI 操作）。
